# Dependências dos serviços de deploy (API + Streamlit).
# Dependências do pipeline de dados/modelo (pandas, lightgbm, scikit-learn)
# ficam em ../../requirements.txt — este arquivo soma só o que falta para
# servir o modelo via HTTP/UI.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
streamlit>=1.33
requests>=2.31
joblib>=1.3
pandas>=2.0
numpy>=1.24
lightgbm>=4.0
scikit-learn>=1.3
